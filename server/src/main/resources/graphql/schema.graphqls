type Query {
    bookById(id: ID): Book
    employeeById(id: ID): Employee
    employees: [Employee]
}

type Employee {
    id: ID
    firstName: String
    lastName: String
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}